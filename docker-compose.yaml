version: '3'
services:
  run:
    build:
      context: .
      dockerfile: Dockerfile
    image: eis-app
    volumes:
      - ./data:/app/data
    command: >
      theguardian
      --query nuclear power
      --from 2022-01-01
      --to 2023-01-01
      --output-articles data/articles.json
      --output-terms data/terms.json
      --count-articles 10
      --count-terms 50
      --api-key <API_KEY>